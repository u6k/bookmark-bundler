<?xml version="1.0" encoding="UTF-8"?>
<project name="${project.name}">
    <body>
        <links>
            <item name="${project.name}" href="${project.url}" />
        </links>
        <menu name="Menu">
            <item name="REST API仕様" href="api-reference.html" />
            <item name="サイトマップ" href="images/sitemap.svg" />
        </menu>
        <menu ref="parent" />
        <menu ref="modules" />
        <menu ref="reports" />
    </body>
</project>
